#include "main.h"

/**
 * mul - mutiplies two integers
 * @a: first integer
 * @b: second integer
 * Return: 0 (success)
 */

int mul(int a, int b)
{
	int a;
	int b;

	_putchar ("%d", (a * b);
			return (0);
			}
